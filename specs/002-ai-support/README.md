# Feature 002 — AI Support Assistant

This is a Spec Kit feature pack for the AI support/search lane. It is intentionally separate from
the active retail implementation feature `001-telegram-shop-mvp`; it must not replace the active
`.specify/feature.json` until the owner/Claude is ready to implement it.

Execution order after the current payment foundation is stable:

```text
spec.md → plan.md → research.md/contracts/data-model → checklists → tasks → analyze → implement
```

